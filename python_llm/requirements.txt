# Install torch first from the nightly cu128 index (required for RTX 50-series / Blackwell GPUs):
#   pip install --pre torch --index-url https://download.pytorch.org/whl/nightly/cu128
# Once a stable PyTorch release supports sm_120, replace with: torch>=2.X.0

accelerate>=1.6.0
bitsandbytes>=0.49.1
langchain>=0.3.24
langchain-huggingface>=0.1.2
pydantic>=2.11.1
python-dotenv>=1.1.0
textstat>=0.7.5
transformers>=4.50.3
